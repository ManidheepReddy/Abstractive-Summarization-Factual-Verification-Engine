[project]
name = "summarization-engine"
version = "0.1.0"
description = "Domain-Adapted Abstractive Summarization & Hallucination Auditing Engine"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
